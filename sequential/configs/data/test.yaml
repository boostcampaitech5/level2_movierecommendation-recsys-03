# data version
data_version: V1

# batch size of DataLoader
batch_size: 70

# pretrain batch size of DataLoader
pre_batch_size: 512

# max sequence length
max_seq_length: 50

# pretrain dataset augmentation
pre_data_augmentation: True

# prepare data at init
prepare_data: True

# todo: fill
mask_p: 0.2

# reserved keys (don't modify!)
item_size: 
mask_id: 
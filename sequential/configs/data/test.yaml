# data version
data_version: Ml

# batch size of DataLoader
batch_size: 70

# pretrain batch size of DataLoader
pre_batch_size: 512

# max sequence length
max_seq_length: 50

# mask prob
mask_p: 0.2

# reserved keys (don't modify!)
item_size: 
mask_id: 
attr_size: 
item2attr: 